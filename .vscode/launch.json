{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Program",
      //"${workspaceRoot}/server/operations/database/eClassKeywordsImport.js"
      // "program": "${workspaceRoot}/server/operations/NPL/stringAlgo.js"
      "program": "${file}"
      // another file ...
      // another file ...
    }
  ]
}
